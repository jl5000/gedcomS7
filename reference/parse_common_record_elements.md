# Parse common elements into a record object

Parse common elements into a record object

## Usage

``` r
parse_common_record_elements(rec, rec_lines)
```

## Arguments

- rec:

  An S7 record object.

- rec_lines:

  A character vector of lines of a GEDCOM record.

## Value

The S7 record object with common elements added as properties.
