# Create a boolean property

Constructs a logical property with a default value and validation to
ensure it contains exactly one element.

## Usage

``` r
prop_bool(default = FALSE)
```

## Arguments

- default:

  Logical value to use as the default.

## Value

An S7 property object.
